{
  "name": "ts_todo_upd",
  "version": "1.0.0",
  "private": true,
  "description": "Simple Kanban-style TODO board in TypeScript with localStorage.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "clean": "powershell -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\"",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
